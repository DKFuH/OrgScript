# OrgScript Formatter

This package will contain formatting logic for OrgScript source files.

Responsibilities:

- normalize indentation
- normalize spacing and blank lines
- keep output deterministic
- preserve semantics
