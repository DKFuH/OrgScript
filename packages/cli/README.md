# OrgScript CLI

This package will contain the `orgscript` command-line interface.

Planned commands:

- `validate`
- `format`
- `lint`
- `export json`

The initial implementation should stay thin and delegate parsing and analysis to the dedicated packages.
