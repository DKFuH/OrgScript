# OrgScript Parser

This package will contain the OrgScript lexer, parser, and AST types.

Responsibilities:

- tokenize OrgScript source
- parse top-level blocks and statements
- produce canonical AST structures
- report syntax errors with line references
